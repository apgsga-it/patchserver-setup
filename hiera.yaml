version: 5
hierarchy:
  - name: "Common Data"
    path: "common.yaml"
  - name: "Environment"
    path: "environment/%{facts.environment}/config.yaml"
defaults:
  data_hash: yaml_data
  datadir: downloads/hiera

