name: test
targets:
  - appl-patch.apgsga.ch
config:
  transport: ssh
  ssh:
    run-as: root
vars:
 # Local Test Vm specific configuration properties
  piper_bind_address:  localhost
  piper_cvs_remote_host: cvs-t.apgsga.ch
  piper_spring_profiles: live,remotecvs, patchOMat
  install_ssh_user: apg_install # TODO (che, 11.2 ) Needs to be adopted
  install_ssh_pw: apg_install # TODO (che, 11.2 ) Needs to be adopted
  artifactory_repo: test-repo
  cm_db_host: chti212.apgsga.ch
  cm_db_instance: chti212
  cm_db_piper_user: cm_example_user
  cm_db_piper_pw: cm_example_user_pw
  yum_repo: multiservice_yumdev
  pipeline_mail_env: JENKINS-TEST

