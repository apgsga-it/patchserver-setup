name: prod
targets:
  - appl-patch.apgsga.ch
config:
  transport: ssh
  ssh:
    run-as: root
vars:
  admin_user_email : it.architektur@apgsga.ch
  piper_bind_address:  localhost # TODO verify
  piper_cvs_remote_host: cvs.apgsga.ch
  piper_spring_profiles: live,remotecvs,patchOMat
  artifactory_repo: repo
  cm_db_host: chti212.apgsga.ch
  cm_db_instance: chti212
  cm_db_piper_user: cm_example_user # TODO  TODO (che, 10.2)  -> hiera?
  cm_db_piper_pw: cm_example_user_pw # TODO  TODO (che, 10.2)  -> hiera?
  yum_repo: multiservice_yumprod
  pipeline_mail_env: JENKINS-PROD
